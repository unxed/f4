# Лунобот-2: тикет #368

## Статус

- [x] Полностью прочитаны тело и переписка тикета; учтана инструкция автора про `[System] ANSICodePage` и `OEMCodePage`.
- [x] Проверен актуальный `main`: исправление уже присутствует в коммите `3a9ddd2e`.
- [x] `cmd/f4/config.go` читает оба ключа из секции `[System]`, применяет их через `vfs.SetSystemCodepages` и сохраняет при записи настроек.
- [x] `vfs/codepages_forced_test.go` покрывает принудительные ANSI/OEM-кодировки, восстановление автоопределения и отклонение неизвестного идентификатора.
- [x] Для коммита `3a9ddd2e` проверены GitHub Actions: пакет `vfs` завершился успешно; общий прогон не зелёный из-за отдельного падения `TestKeyRemap_ExactRulesAndAreaPrecedence` (`f1` против `F1`) в `cmd/f4`, не связанного с этой задачей.
- [ ] Автор подтвердил результат на своей системе.

## Решения и инварианты

- Пустое значение, `0` и `auto` оставляют системное автоопределение; неверное значение не заменяет рабочую кодировку неизвестным идентификатором.
- ANSI и OEM настраиваются независимо: ошибочный идентификатор одной стороны не сбрасывает корректное значение другой.
- Настройка влияет на существующие псевдонимы ANSI/OEM, меню и определение кодировок через общий слой `vfs`; отдельного дублирующего пути в `cmd/f4` нет.
- Новый production-код не создаётся: реализация уже находится в `main`; этот follow-up только фиксирует её состояние и результаты CI.

## Проверка

Локальные сборки и тесты не запускались по инструкции Лунобота-2. Проверены исходники актуального `main`, тестовый файл и результаты GitHub Actions; автору оставлена инструкция проверки, тикет не закрыт.
