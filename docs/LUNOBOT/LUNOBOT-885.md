# Лунобот-2 (instance a721a6d1487257292ae00780 LNX): тикет #885

## Статус

- [x] Полностью прочитаны тело и переписка тикета; изучены связанные upstream-ссылки и текущие документы ConPTY в репозитории.
- [x] В `DISPATCH.md` зарегистрирована часть 1 из 2.
- [x] Зафиксировано решение автора: не применять эвристику склейки строк, а использовать согласованную современную пару `conpty.dll` + `OpenConsole.exe`.
- [x] У автора запрошены критерии первой атомарной части: архитектура, точный пакет и проверяемый путь F3/F4.
- [ ] Получены и зафиксированы критерии готовности.
- [ ] Реализовано подключение согласованной пары к f4 отдельным PR.
- [ ] GitHub Windows CI подтвердил длинные логические строки и F3/F4 handoff.
- [ ] PR первой части объединён в `main`; в тикете оставлена инструкция проверки.

## Решения и инварианты

- Нельзя восстанавливать логические строки эвристикой по ширине строки и CRLF: это может незаметно склеить настоящие строки или потерять данные.
- `conpty.dll` и `OpenConsole.exe` должны поставляться согласованной парой из одного проверенного пакета; подмена только одного файла недопустима.
- Поведение реального Windows и минимальная версия ОС должны быть подтверждены GitHub Windows CI; локальные сборки и тесты не запускаются.
- Документы `docs/CONPTY_LINE_WRAP_FINDINGS.md`, `docs/CONPTY_GATE_REQUIREMENTS.md` и `docs/CONPTY_NATIVE_TEST.md` описывают предпосылки и измерения, но сами по себе не доказывают интеграцию пары в f4.

## Проверка

Локальные сборки и тесты не запускались по инструкции Лунобота-2. Этот коммит только фиксирует статус и запрос критериев; реализация и платформенная проверка начнутся после подтверждения точного объёма первой части.
