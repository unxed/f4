# Лунобот-2: тикет #915

## Статус

- [x] Полностью прочитано тело тикета; комментариев и связанных PR в тикете не найдено.
- [x] В `DISPATCH.md` зарегистрирована часть 1 из 2.
- [x] У автора запрошены критерии первой атомарной части, набор форматов и примеры для CI.
- [ ] Получены и зафиксированы критерии готовности.
- [ ] Реализована первая атомарная часть отдельным PR.
- [ ] GitHub Actions для PR первой части завершились успешно; PR объединён в `main`.
- [ ] В тикете оставлено сообщение с краткой инструкцией проверки.

## Решения и инварианты

- Исходный запрос объединяет несколько независимых возможностей: вход в SFX/контейнеры, просмотр, извлечение, изменение содержимого и проверку ошибок.
- До подтверждения форматов и границ первой части нельзя выбирать библиотеку, формат контейнера или поведение read-only/повреждённых архивов.
- Первая часть должна быть пользовательски заметной и иметь отдельные проверки в GitHub Actions; локальные сборки и тесты не запускаются.

## Проверка

Локальные сборки и тесты не запускались по инструкции Лунобота-2. Этот коммит только фиксирует статус и запрос критериев; проверка реализации будет выполнена через GitHub Actions после согласования объёма.
